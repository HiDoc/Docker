docker run -it --rm  \
	--name Abathur \
	-v ~/flask:/flask \
	-w /flask/ \
	-p 3000:3000 \
	-e FLASK_APP=flaskio.py \
	python:2-slim \
bash
#run : pip install flask && flask run --port=3000 --host=0.0.0.0
